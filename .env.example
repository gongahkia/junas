# base64 Fernet key, generate with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
KT_CRED_KEY=replace-with-fernet-key

KT_DB_PATH=./data/kt.db
KT_LOG_LEVEL=info
KT_HOST=0.0.0.0
KT_PORT=8000
