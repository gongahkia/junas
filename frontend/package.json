{
  "name": "caipng-frontend",
  "version": "1.0.0",
  "description": "Frontend for cAIpng dish recommendation system",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.4",
    "vite": "^4.4.9",
    "eslint": "^8.49.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo 'No frontend tests for V2 prototype' && exit 0",
    "lint": "eslint src --ext js,jsx || true",
    "lint:fix": "eslint src --ext js,jsx --fix || true"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}