# Cover Letter Template

Subject: Independent reproduction of SG-LegalBench v0.1 receipts

Dear <Name>,

I am writing to ask whether <Institution> would be willing to run an
independent reproduction of SG-LegalBench v0.1, an open Singapore legal-LLM
benchmark built from public-domain sources with mechanically derived labels.

The request is deliberately small: one ML engineer runs the eligible v0.1 tasks
against a model selected by <Institution>, keeps the JSON receipts, and publishes
the receipt bundle under `runs/external/<institution>/` if your review process
approves. The receipt includes the model label, prompt provenance, bootstrap
confidence intervals, and contamination-probe summary, so the published result
can be inspected without trusting our own baseline run.

We expect the work to take 4-6 weeks end to end, including review time. The
attached kit includes install commands, task selection, compute estimates, and
the publication contract. We would credit <Institution> on the leaderboard and
in the reproduction notes; substantive methodology review or error analysis
would also be considered for v0.2 preprint co-authorship.

Would you be open to a 30-minute scoping call in the next two weeks to confirm
fit, model choice, and publication approvals?

Best,

<Sender>

<Role / affiliation>

<Email>

Attachments:

- `docs/outreach/independent-reproduction-kit.md`
- `docs/outreach/three-targets.md`
