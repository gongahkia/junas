# API Schema Notes

The live contract is generated by FastAPI at `/openapi.json`. Regenerate examples with:

```sh
uv run python scripts/export_openapi_examples.py
```

## Current Product Surfaces

- `POST /review`: deterministic PII/MNPI review over inline text or base64 TXT/DOCX/PDF.
- `POST /anonymize`: same review plus deterministic placeholder replacement.
- `POST /reidentify`: restores placeholders from a caller-supplied mapping or persisted document hash.
- `POST /documents/scrub`: metadata scrub for supported document/image formats.
- `POST /classify`, `POST /classify/batch`: compatibility wrappers over `engine.review()`.

## Classify Compatibility

`ClassifyResponse` retains old fields (`lexicon`, `model1`, `model2`, `embedding`, `clustering`, `mosaic`, `regression`, `offending_spans`) for client compatibility. Under the pivoted runtime they are always `null`; current clients should consume:

- `classification`
- `pii_score`
- `mnpi_score`
- `findings`
- `coverage_warnings`
- `observability`

## Runtime Status

- `/ready` exposes configured optional layers and readiness.
- `/diagnostics` exposes loaded layers, startup timings, dependency status, and runtime layer errors.
- Public evidence and LLM dependencies report disabled/configured status; Redis is no longer part of the active runtime.
