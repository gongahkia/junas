(B) In privacy-tui/src/main.rs cmd_run(), call spawn_pipeline() with a MacosCaptureSource (Display(0) default or window from app.selected_window_id), create the output sink via autodetect::detect_best_sink, wire the transformed_rx to the sink, and pass both the PipelineHandle and sink to ordered_shutdown() instead of None,None. +pipeline +infra
(B) In privacy-tui/src/main.rs cmd_run(), after spawning the pipeline, spawn a background thread that reads TransformedFrames from the pipeline's output channel and updates app.tx_preview_pixels and app.stats each tick, so the TUI braille previews and stats bar display real data. +pipeline +infra
(B) In privacy-core/src/detection/expand.rs line 36, fix the severity comparison: the Severity enum has High=0,Medium=1,Low=2 by declaration order, so `as u8` comparison is inverted. Replace with an explicit ordering method (e.g. fn rank(&self)->u8 { match self { High=>2, Medium=>1, Low=>0 } }) and use rank() in the comparison so High is correctly kept over Low. +detection +fix
(B) In privacy-tui/src/app.rs App::new(), call AppConfig::load() (from privacy_core::config) and apply loaded values to transform_mode, transform_intensity, and profile_names so config is honoured at startup instead of hardcoded defaults. +config +fix
(B) In privacy-core/src/pipeline_runner.rs spawn_pipeline(), after loading AppConfig, pass config.detection.min_confidence as a f32 to OcrEngine::new_with_confidence() (add that constructor to ocr.rs) instead of using the hardcoded MIN_CONFIDENCE=60.0 constant. +detection +config +fix
(B) In privacy-core/src/detection/ocr.rs, add an OcrEngine::new_with_confidence(data_path, min_conf: f32) constructor that stores min_conf as a field and uses it in extract() instead of the hardcoded 60.0 constant, so DetectionConfig::min_confidence is actually respected. +detection +config +fix
(B) In privacy-tui/src/app.rs App::new(), call privacy_core::detection::user_patterns::load_user_patterns() and extend pattern_registry.patterns with the result so user-defined patterns from ~/.config/ascii-privacy/patterns.toml are active at runtime. +detection +config
(B) In privacy-core/src/detection/scanner.rs scan(), load the Whitelist (or accept it as a parameter) and skip any TextRegion whose text matches whitelist.is_safe() before pattern matching, so whitelist.toml entries are actually respected. +detection +config
(B) In privacy-tui/src/app.rs App::new() or at pipeline start, call privacy_core::detection::whitelist::Whitelist::load() and store it in App or SharedState so it can be passed to the scanner. +detection +config
(B) In privacy-tui/src/main.rs cmd_run(), when the user changes app.selected_window_id via the window selector, stop the current pipeline and restart it with a new MacosCaptureSource targeting the selected window id, so window selection actually switches the capture target. +pipeline +feature
(B) In privacy-tui/src/main.rs handle_event(), in the KeyCode::Char('r') branch, instead of only toggling app.recording_started_at, call Recorder::start() (from privacy_output::recorder) with the current frame width/height and an auto-named output path when starting, and call recorder.stop() when stopping; store the Recorder instance in App. +feature +output
(B) In privacy-tui/src/app.rs, add a field recorder: Option<privacy_output::recorder::Recorder> to App and update push_log (or a new tick method) to call recorder.write_frame() when recording is active and a new TransformedFrame arrives. +feature +output
(B) In privacy-tui/src/main.rs cmd_test_screen(), replace the blank synthetic frame (vec![0u8; 640*480*4]) with a real frame captured from the first window returned by list_windows() using MacosCaptureSource::start()/next_frame(), so the subcommand tests real screen content. +fix +detection
(B) In privacy-core/src/transform/neural.rs, replace the static SESSION: OnceLock with a static SESSION: Mutex<Option<NeuralStyleTransfer>> so that if the model is absent on first use, a subsequent call after the user downloads the model can succeed without restarting the process. +fix +stability
(B) In privacy-core/src/transform/model_download.rs, replace the all-zeros MODEL_SHA256 placeholder with the real SHA-256 of the animegan_v2.onnx file (compute with sha256sum after a test download) so integrity verification is meaningful. +fix +security
(B) In privacy-core/src/pipeline_runner.rs spawn_pipeline(), read config.transform.accelerator and call privacy_core::transform::neural::configure_accelerator(config.transform.accelerator) before the first neural inference, so the ONNX execution provider preference from config is actually applied. +config +fix
(B) In privacy-core/src/pipeline_runner.rs detection thread, after scan+expand_and_merge, call line_expand::expand_to_end_of_line(merged, frame.width) on the result before constructing DetectedRegions, so partial-line leaks (e.g. SECRET_KEY=abc showing truncated) are fully redacted to EOL. +detection +feature
(B) In privacy-core/src/detection/incremental.rs IncrementalOcr::extract(), replace the ad-hoc hash_cell_pixels with FrameDiff (from frame_diff.rs) so the more sophisticated pixel-threshold dirty-cell logic is actually used, eliminating the duplicate implementation and removing the dead code. +refactor +detection
(B) In privacy-tui/src/tui_ui or app, add a keybinding (e.g. 'f') that calls privacy_core::detection::training::save_false_positive() on the last detection in app.log_entries, so streamers can mark false positives for later pattern improvement. +feature +detection
(B) In privacy-tui/src/control_server.rs run_server(), add a connection semaphore or AtomicUsize connection counter capped at 10 (configurable) so that unbounded thread spawning per connection is prevented. +security +stability
(B) In privacy-tui/src/main.rs export_session_log(), replace the hardcoded `aki_session_{ts}.json` CWD path with a path inside the user's config dir (e.g. ~/.config/ascii-privacy/sessions/) using the same XDG logic as config_path(), so exports land predictably. +fix +output
(B) In privacy-core/src/detection/user_patterns.rs load_from_path(), replace Box::leak(entry.name.into_boxed_str()) with an owned-name variant of SensitivityPattern (change name: &'static str to name: Cow<'static, str> or String) to eliminate the permanent memory leak on each reload. +fix +stability
(B) In privacy-tui/src/ui/stats_bar.rs (or wherever pipeline stats are rendered), surface the adaptive-quality grid scale and dropped frame count from SharedState so users can see when OCR quality is being reduced under load. +dx +observability
(B) In privacy-core/src/pipeline_runner.rs capture thread, when source.start() returns Err, send the error message through a dedicated error channel back to the TUI instead of silently returning, so the stats bar can display a "capture failed" message. +stability +dx
(B) In privacy-core/src/pipeline_runner.rs or SharedState, expose a dropped_frames AtomicU64 counter that increments each time a frame is dropped in the capture or transform thread due to full channels, and wire it to app.stats.dropped_frames for display. +observability +stability
(B) In privacy-tui/src/main.rs cmd_headless(), replace the bare AtomicBool loop with a real headless pipeline: call spawn_pipeline() with the default capture source and output sink (same as cmd_run but without TUI), log stats to a file, and stop on Ctrl-C, fulfilling the planned stretch goal. +feature +output
(B) In privacy-tui/src/app.rs switch_profile(), after updating active_profile, actually apply the profile's transform_mode and intensity overrides from AppConfig::profiles[name] to app.transform_mode and transform_intensity, so numbered keybindings 1-9 have visible effect. +feature +config
(B) In privacy-core/src/transform/neural.rs apply_neural(), change the NEURAL_LATENCY_GUARD_MS from a hardcoded 30ms to a value read from config (or a reasonable default of 100ms for CPU inference) since 30ms will nearly always trigger cartoon fallback on CPU. +fix +config
(B) In privacy-tui/src/ui/stats_bar.rs, display the active neural model status ("neural: ready"/"downloading"/"unavailable") next to the transform mode indicator so users understand why Neural mode might be falling back to cartoon. +dx +feature
(B) In privacy-tui/src/ui/window_selector.rs or pattern_manager, display PatternCategory alongside pattern name in the pattern manager UI so users can see which category (Token, ApiKey, PII, etc.) each pattern belongs to. +dx +feature
(B) In privacy-core/src/detection/scanner.rs, make scan() accept the Whitelist by reference and skip matches where whitelist.is_safe(region.text) is true, consolidating the whitelist check inside the scan function rather than requiring callers to pre-filter. +detection +refactor
(B) In privacy-tui/Cargo.toml and privacy-tui/src/main.rs, add the ctrlc crate (or handle SIGTERM via signal-hook already in Cargo.lock) for graceful shutdown in both headless and TUI mode so Ctrl-C triggers ordered_shutdown instead of an abrupt kill. +stability +infra
(B) In privacy-output/src/obs_websocket.rs, verify the OBS WebSocket output path is reachable from cmd_run (check if create_sink handles SinkKind for OBS or if it is dead code) and either wire it into the sink selection or remove the dead module. +infra +output
(B) In privacy-output/src/tray.rs, verify the system tray module is wired to the app lifecycle on macOS (check for a spawn call in cmd_run) and if not, add a tray::spawn() call after pipeline start so macOS users get a status-bar icon showing redaction state. +feature +output
(B) In privacy-core/src/detection/default_patterns.rs, add a pattern for Hugging Face API tokens (hf_[a-zA-Z0-9]{34,}) and Anthropic API keys (sk-ant-[a-zA-Z0-9\-_]{90,}) since these are high-value credentials commonly visible during live coding sessions. +detection +feature
(B) In privacy-core/src/detection/pii_patterns.rs, add a pattern for credit card numbers (Luhn-format 13-19 digit sequences) at High severity since streamers sharing financial dashboards are a key user scenario. +detection +feature
(B) In privacy-core/src/detection/pii_patterns.rs, add a pattern for private SSH keys (PEM block endings, -----END ... PRIVATE KEY-----) to complement the existing BEGIN header pattern so the full key block is flagged. +detection +fix
(B) In privacy-core/src/capture/region.rs, verify the CaptureRegion logic and ensure it is wired to DetectionConfig::region ("x,y,w,h" string) via parse_rect() at pipeline startup so users can restrict capture to a sub-region of the screen. +feature +config
(B) In privacy-core/src/transform/blur.rs and pixelate.rs, ensure the sigma/block-size scales with the `intensity` parameter (0.0=none, 1.0=maximum) so the '+'/'-' keys in the TUI produce a perceptible gradient of blur strength, not an on/off toggle. +fix +feature
(B) In privacy-tui/src/ui/detection_log.rs, truncate log entries display to the last 20 entries with scrolling (j/k) rather than only showing the last N lines statically, so users can review historical detections without losing context. +dx +feature
(B) In privacy-tui/src/ui/heatmap.rs, cap the heat color scale legend at the UI so users understand what "hot" means (e.g. 30+ hits/60s = red) rather than rendering unlabeled color gradients. +dx +feature
(B) In privacy-tui/src/main.rs handle_event(), add a '?' keybinding that renders a full keybinding help overlay listing all shortcuts (w=window, p=patterns, space=pause, t=cycle, +/-=intensity, h=heatmap, s=stats, r=record, e=export, f=false-positive, 1-9=profiles, ?=help, q=quit). +dx +feature
(B) In privacy-output/src/twitch.rs, check if Twitch RTMP output is wired into the sink selection pipeline and if not, either wire it (add SinkKind::Twitch) or mark it as a planned feature with a stub error, since streamers on Twitch are a primary persona. +output +feature
(B) In privacy-core/src/capture/pty.rs, check if PTY capture (capturing terminal text directly without OCR) is wired into the pipeline as an alternative capture source and if so, expose it via a CLI flag or config option since it would give near-zero-latency detection for terminal-only streamers. +feature +detection
(B) In privacy-core/src/detection/mod.rs, re-export FrameDiff from frame_diff.rs so it is accessible to external callers (currently it may only be used internally), in preparation for wiring it into IncrementalOcr. +refactor +detection
(B) In privacy-tui/src/ui/stats_bar.rs, display the currently active output sink kind (v4l2/coremedia/mjpeg) and its port or device path so users can verify their virtual camera is connected before going live. +dx +observability
