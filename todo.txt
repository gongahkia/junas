(B) In privacy-tui/src/ui/detection_log.rs, truncate log entries display to the last 20 entries with scrolling (j/k) rather than only showing the last N lines statically, so users can review historical detections without losing context. +dx +feature
(B) In privacy-tui/src/ui/heatmap.rs, cap the heat color scale legend at the UI so users understand what "hot" means (e.g. 30+ hits/60s = red) rather than rendering unlabeled color gradients. +dx +feature
(B) In privacy-tui/src/main.rs handle_event(), add a '?' keybinding that renders a full keybinding help overlay listing all shortcuts (w=window, p=patterns, space=pause, t=cycle, +/-=intensity, h=heatmap, s=stats, r=record, e=export, f=false-positive, 1-9=profiles, ?=help, q=quit). +dx +feature
(B) In privacy-output/src/twitch.rs, check if Twitch RTMP output is wired into the sink selection pipeline and if not, either wire it (add SinkKind::Twitch) or mark it as a planned feature with a stub error, since streamers on Twitch are a primary persona. +output +feature
(B) In privacy-core/src/capture/pty.rs, check if PTY capture (capturing terminal text directly without OCR) is wired into the pipeline as an alternative capture source and if so, expose it via a CLI flag or config option since it would give near-zero-latency detection for terminal-only streamers. +feature +detection
(B) In privacy-core/src/detection/mod.rs, re-export FrameDiff from frame_diff.rs so it is accessible to external callers (currently it may only be used internally), in preparation for wiring it into IncrementalOcr. +refactor +detection
(B) In privacy-tui/src/ui/stats_bar.rs, display the currently active output sink kind (v4l2/coremedia/mjpeg) and its port or device path so users can verify their virtual camera is connected before going live. +dx +observability
