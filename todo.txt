[] edit schema.md to standardise labels --> "non" for non-sensitive sentences, "high" for high-sensitivity sentences, and "low" for non-sensitivity sentences
[] add inference-time encoder to embeddings/ -- SentenceTransformer.encode(text) callable at runtime so IF can be wired into api/main.py (currently embeddings/generate_embeddings.py is offline batch only)

