(B) Remove unimplemented API endpoints (Users, Preferences, Favorites, Dishes search) from README.md API Reference section +refactor
(B) Replace hardcoded MongoDB credentials in docker-compose.yml with environment variable references and update .env.example +security
(B) Add multi-stage production Dockerfile for frontend: build stage with vite build, serve stage with nginx:alpine +infra
(B) Add MongoDB connectivity check to GET /health endpoint in server.js (return degraded status if DB unreachable) +infra
(B) Fix button disabled logic bug in LiveView.jsx line 365: change avgByLabel.length to Object.keys(avgByLabel).length +feature
(B) Add confidence threshold filter (0.4 minimum) in LiveView.jsx overlay rendering to suppress low-confidence noise +feature
(B) Add proper .env.example files for both backend and frontend with all required variables documented +infra
(B) Add structured error response from macros endpoint when Gemini returns placeholder zeros so frontend can distinguish error from real result +feature
