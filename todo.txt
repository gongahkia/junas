(C) Implement frame diff optimization: compute pixel-level diff between consecutive frames, only re-run OCR and transformation on changed regions — potential 50-80% speedup for typical terminal sessions +infra
(C) Add startup self-test: on first run, display test frame with known sensitive patterns, verify detection pipeline catches them all, report readiness score +cli
(C) Implement transition animation between transformation modes: when user switches mode via `t`, crossfade between old and new transformation over 10 frames for smooth visual transition +transform
(C) Add remote control API: localhost WebSocket server accepting commands (pause, resume, switch_mode, get_stats) for integration with Stream Deck or custom control panels +output
(C) Add under reference section of README.md that the name is in reference to the japanese 空き (Aki) which roughly means Empty, vacant, or a gap. (You’ll see this in 空き容量 (aki yōryō), which means free disk space.)
