(A) Implement ASCII-art transformation for sensitive regions: convert pixel block to average luminance, map to ASCII character density ramp (` .,:;i1tfLCG08@`), render ASCII characters back as pixels using embedded monospace font bitmap +transform
(A) Implement pixelation transformation for sensitive regions: downscale region to 1/8 resolution using nearest-neighbor, upscale back to original size — fastest option, ~0.1ms per region +transform
(A) Implement blur transformation for sensitive regions: apply Gaussian blur with configurable sigma (default 15.0) using separable horizontal+vertical passes for O(n) performance +transform
(A) Create transformation registry: enum TransformMode {Cartoon, Ascii, Pixelate, Blur} with factory returning trait object implementing `fn apply(region: &mut ImageRegion)`, selected via config +transform
(A) Implement selective region compositor: accept full frame + list of SensitiveMatch bounding boxes + selected TransformMode, apply transformation only to matched regions, leave rest of frame untouched +transform
(A) Add transformation intensity control: configurable strength parameter (0.0-1.0) that blends between original and transformed pixels — 1.0 = full transformation, 0.5 = partial overlay for softer effect +transform
(A) Implement virtual camera output on Linux using v4l2loopback: open `/dev/video<N>` loopback device, write transformed RGBA frames converted to YUYV format at capture FPS +output
(A) Implement virtual camera output on macOS using CoreMediaIO virtual device via `camera-virtual` approach: create DAL plugin-based virtual camera, feed transformed frames as CMSampleBuffers +output
(A) Implement fallback output: write transformed frames as MJPEG to a localhost HTTP endpoint (`http://127.0.0.1:9876/stream`) consumable as browser source in OBS — universal fallback for all platforms +output
(A) Implement output sink abstraction: trait OutputSink with `fn write_frame(frame: &TransformedFrame)`, implementations for v4l2loopback, CoreMediaIO, and HTTP MJPEG, selected by platform + config +output
(A) Add auto-detection of virtual camera availability: on Linux check for `/dev/video*` loopback devices, on macOS check for DAL plugin, fall back to HTTP MJPEG if neither available, log selected output method +output
(A) Implement Ratatui TUI scaffold: async tokio runtime, event loop handling keyboard input (crossterm), tick events for UI refresh (10Hz), and frame pipeline stats updates +tui
(A) Build TUI layout with 4 zones: top bar (app name + FPS counter + output status), left pane (live raw capture preview as half-resolution braille art), right pane (live transformed output preview as braille art), bottom panel (detection log + controls + status) +tui
(A) Implement raw capture preview widget: downscale captured frame to fit TUI pane dimensions, render as unicode braille characters (2x4 dot patterns via `⠀`-`⣿` block) with 256-color approximation, update at 10 FPS +tui
(A) Implement transformed output preview widget: same braille rendering as raw preview but showing post-transformation frame, displayed side-by-side with raw for comparison +tui
(A) Implement detection log widget: scrollable list in bottom panel showing last 50 detected sensitive regions with timestamp, pattern name, severity badge (colored), and truncated matched text (first 4 chars + "***") +tui
(A) Implement real-time stats display in top bar: actual capture FPS, processing latency (ms per frame), OCR latency, transformation latency, output write latency, dropped frame count +tui
(A) Implement keybinding `Space` to toggle pipeline on/off (pause capture + output), `t` to cycle transformation mode (cartoon→ascii→pixelate→blur), `+/-` to adjust transformation intensity +tui
(A) Implement window selector view: keybinding `w` opens overlay listing available windows from window picker module, j/k to navigate, Enter to select, applies immediately +tui
(A) Implement pattern manager view: keybinding `p` opens overlay listing all active sensitivity patterns with name, regex preview, severity, enabled toggle — Space to enable/disable pattern, changes take effect immediately +tui
(A) Implement config file at `~/.config/ascii-privacy/config.toml`: sections `[capture]` (source, window_title_match, fps, region), `[detection]` (ocr_engine, min_confidence, grid_cells), `[transform]` (mode, intensity), `[output]` (sink, http_port, v4l2_device) +cli
(A) Implement CLI entrypoint using `clap`: subcommands `run` (launch TUI + pipeline), `list-windows` (print available windows), `test-patterns <text>` (run patterns against input), `check-output` (verify virtual camera availability) +cli
(A) Wire full pipeline: capture thread → detection thread → transformation thread → output thread, connected via bounded crossbeam channels (capacity 3 frames), backpressure drops oldest frame +infra
(A) Implement graceful shutdown: Ctrl+C or `q` in TUI triggers ordered shutdown — stop capture → drain pipeline → flush last frame to output → close virtual camera device → exit +cli
(B) Implement ML-based neural style transfer transformation using `ort` crate (ONNX Runtime): load AnimeGAN v2 ONNX model (~8MB), run inference on sensitive regions, stylize into anime/cartoon aesthetic +transform
(B) Implement ONNX model download on first use: check `~/.cache/ascii-privacy/models/animegan_v2.onnx`, download from GitHub releases if missing, verify SHA-256 checksum +transform
(B) Add GPU acceleration for neural style transfer: configure ONNX Runtime execution provider — CUDA on Linux, CoreML on macOS, fallback to CPU — selectable via `[transform].accelerator` config +transform
(B) Implement neural style transfer latency guard: if inference takes >30ms per region (risking frame drops), auto-fallback to algorithmic cartoon for remainder of that frame, log warning +transform
(B) Add adaptive quality scaling: if pipeline latency exceeds frame budget (33ms at 30FPS), automatically reduce OCR grid resolution and transformation quality until pipeline meets target +infra
(B) Implement multi-region parallel transformation: process multiple detected sensitive regions concurrently using rayon parallel iterator, merge results back into frame +transform
(B) Add "safe zone" configuration: user defines screen regions (as rects) that are NEVER redacted (e.g., bottom status bar of terminal), even if patterns match within them +detect
(B) Add "always redact zone" configuration: user defines screen regions that are ALWAYS transformed regardless of pattern matches (e.g., top 3 lines where shell prompt shows env) +detect
(B) Implement sensitivity heatmap overlay mode: keybinding `h` toggles heatmap overlay on raw preview showing detection frequency per region (red=frequent matches, blue=never matched) over last 60 seconds +tui
(B) Add detection statistics view: keybinding `s` shows overlay with total detections by pattern, false positive rate estimate, regions per frame average, busiest screen zones +tui
(B) Implement frame recording: keybinding `r` toggles recording of transformed output to local MP4 file using `ffmpeg` subprocess with pipe input, display recording indicator in top bar with elapsed time +output
(B) Add OBS WebSocket integration: connect to OBS WebSocket API (v5), auto-create Browser Source pointing to HTTP MJPEG endpoint, toggle source visibility on pipeline start/stop +output
(B) Implement latency graph widget: small sparkline in stats area showing frame processing latency over last 120 frames, highlight spikes above frame budget in red +tui
(B) Add per-pattern sensitivity tuning: in pattern manager view, allow adjusting severity level and enabling/disabling individual patterns without editing config files +tui
(B) Implement session log export: keybinding `e` writes detection log (all detected sensitive regions with timestamps, pattern, severity) to JSON file for post-session audit +tui
(B) Add `--headless` mode: run pipeline without TUI, output to virtual camera only, log stats to file — useful for running as background service during streams +cli
(B) Implement `test-screen` subcommand: capture 10 frames, run detection, print summary of found sensitive regions — useful for verifying patterns before going live +cli
(B) Add profile system: `[profiles.streaming]`, `[profiles.pairing]` in config with different transform modes and pattern sets, switch via `1-9` keybindings in TUI +cli
(B) Implement color preservation in cartoon transformation: extract dominant colors from region before transformation, re-apply as palette to cartoon output so syntax highlighting "vibe" is preserved +transform
(B) Add partial transparency blending for transformed regions: configurable alpha (default 0.9) so transformed regions have slight ghosted appearance of underlying structure without readable text +transform
(B) Implement notification on first detection: flash border of raw preview red briefly when first sensitive region detected in a new screen state, confirms to user the filter is working +tui
(B) Add system tray indicator on macOS using `tray-item` crate: show green/red dot for pipeline status, click to open TUI or quit — provides visual confirmation filter is running +output
(C) Implement PTY-based capture: instead of screen capture, wrap user's shell in a PTY, intercept terminal output byte stream, render to virtual terminal (using `vt100` crate), capture rendered framebuffer — OS-independent, no screen capture permissions needed +capture
(C) Add Wayland wlr-screencopy protocol support as alternative to PipeWire for wlroots-based compositors (Sway, Hyprland) +capture
(C) Implement audio passthrough: capture system audio via platform API, mix with virtual camera output as virtual microphone for complete OBS source — no sensitive audio content by default +output
(C) Add training data collector: optionally save false-positive regions (user marks as safe via keybinding) to dataset for pattern improvement feedback loop +detect
(C) Implement multi-monitor support: in window selector, show display index for each window, allow selecting capture region spanning specific monitor +capture
(C) Add Twitch chat integration: connect to Twitch IRC, display viewer count and chat messages in TUI sidebar, allow toggling privacy filter on/off via chat command (!filter) +output
(C) Implement frame diff optimization: compute pixel-level diff between consecutive frames, only re-run OCR and transformation on changed regions — potential 50-80% speedup for typical terminal sessions +infra
(C) Add startup self-test: on first run, display test frame with known sensitive patterns, verify detection pipeline catches them all, report readiness score +cli
(C) Implement transition animation between transformation modes: when user switches mode via `t`, crossfade between old and new transformation over 10 frames for smooth visual transition +transform
(C) Add remote control API: localhost WebSocket server accepting commands (pause, resume, switch_mode, get_stats) for integration with Stream Deck or custom control panels +output
(C) Add under reference section of README.md that the name is in reference to the japanese 空き (Aki) which roughly means Empty, vacant, or a gap. (You’ll see this in 空き容量 (aki yōryō), which means free disk space.)