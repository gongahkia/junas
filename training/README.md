# Training Folder

This folder contains the higher-level training and validation entrypoints.

- `train_validate_classification.py`: train and evaluate the classification models
- `train_validate_pipeline.py`: orchestrate full-pipeline training, validation, and artifact generation
