# Configs Folder

This folder contains runtime and evaluation configuration files.

- `runtime.py`: config loading helpers used by the backend
- `eval_*.toml`: evaluation profiles for pipeline benchmarking and validation
- `thresholds.lock.json`: optional generated threshold lock file when present
