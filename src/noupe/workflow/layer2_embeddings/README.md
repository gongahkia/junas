# Layer 2 Embeddings

This folder contains embedding generation code.

- `inference.py`: runtime embedding encoder used by the backend
- `generate_embeddings.py`: offline embedding generation for training workflows
