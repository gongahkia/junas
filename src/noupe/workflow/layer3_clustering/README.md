# Layer 3 Clustering

This folder contains anomaly detection built on Isolation Forest.

- `isolation_forest.py`: training and inference entrypoint
- `checkpoints/`: active clustering artifact directory
