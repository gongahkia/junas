# Archived Training Checkpoints

This folder stores training-time checkpoints that are not required for the active inference runtime.

The canonical runtime checkpoints stay under `backend/workflow/`.
