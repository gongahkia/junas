{{ define "kilter_together.title" -}}
[{{ .Status }}] {{ .CommonLabels.alertname }}
{{- end }}

{{ define "kilter_together.body" -}}
Summary: {{ .CommonAnnotations.summary }}

Description:
{{ .CommonAnnotations.description }}

Severity: {{ .CommonLabels.severity }}
Alert count: {{ len .Alerts }}

Labels:
{{- range .CommonLabels.SortedPairs }}
- {{ .Name }}={{ .Value }}
{{- end }}

Operator drilldown:
1. Open the Kilter Together Grafana dashboard and isolate the affected time window.
2. Query /api/operator/status with the operator token to confirm runtime readiness and provider cache health.
3. Correlate request_id and trace_id from logs, traces, and GlitchTip before remediation.
{{- end }}
